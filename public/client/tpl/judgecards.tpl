<div class="sgs-judge-cards position-absolute d-flex flex-row-reverse">
    {{#judgeStack}}
    <div class="sgs-judge-card badge badge-dark" pk="{{ pk }}">
        <span>{{ shortName }}</span>
    </div>
    {{/judgeStack}}
</div>
